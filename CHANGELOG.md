# CHANGELOG

利用者・保守者から見て意味のある変更を、新しいものほど下へ追記します。

## 2026-09-22

### Changed

- 避難所候補レビュー画面（`review.html`）の候補表を、職員が確認しやすい順序へ整理
  （順位・避難所名 → 距離 → 避難所地点のハザード → 災害種別対応 → 本人と避難所の間のハザード区域）
- 災害種別対応を、記号（○ 対応 / △ 2階以上 / × 非対応）で横方向に並べる表示へ変更。
  すべて対応している場合は「全◯種に対応」と要約し、差異のある種別だけを個別に表示する
- 「直線交差ハザード」を「本人と避難所の間のハザード区域（参考）」へ改称し、避難経路ではない旨の
  説明を表の直下に追加
- ハザード区分を災害種別ごとにまとめて表示（例: `津波：0.01m以上0.3m未満・0.3m以上1.0m未満`）。
  同じ内容の重複は表示しない
- 該当するハザードが無い場合の表示を「なし」から「該当なし」へ変更

### Removed

- サイドバーの「沖縄本島内の位置」（広域位置図）を削除。あわせて概要背景PNG
  （`basemap_okinawa_mainland.png`）の生成も取りやめ、レビューZIPから同梱しないようにした

## 2026-09-23

詳細: [docs/bugfix-2026-09-23.md](docs/bugfix-2026-09-23.md)

### Fixed

- 要支援者CSVの差し替えが入力チェックで止まったあとに後続のセルを実行すると、今回のCSVの
  要支援者の行に、前回のCSVの別人の座標・避難所候補が付いた結果CSVが出力されていた。
  読込に失敗した場合は前回のデータを残さず、候補算出セルで止まるようにした
  （避難所一覧・ハザードデータの読み直しに失敗した場合も同様）。あわせて、設定・入力の各セルを
  実行し直した時点で前回の算出結果を消し、候補算出セルを実行し直すまで出力セルが前回の結果CSVを
  再出力しないようにした
- `ENABLE_HAZARD_CHECK` を `True` から `False` に変えて再計算すると、レビューHTMLにだけ前回の
  ハザード区域が表示されていたのを修正
- レビュー画面（`review.html`）の地図を際限なく拡大・縮小でき、何も映らない状態になっていた。
  ズーム10〜18の範囲に制限した
- ハザード区域が地図の表示範囲に無い場合に、レビュー用HTML作成時の表示が
  「ハザード判定を行っていないため」となっていたのを正しい文言にした

### Changed

- 座標の有効性の判定を `match_status` へ一本化し、到達しない分岐・使われていない確認関数を整理
  （結果CSVは修正前とバイト単位で同一）
- 背景地図の生成処理（`review_builder.py`）を1枚分の処理へ整理。レビューHTMLへ埋め込む
  背景地図情報を `basemap.detail` から `basemap` へ1段浅くした
- CSVの文字コード判定から、到達しない3段目（UTF-8）を削除（読み込めるファイルは変わらない）

## 2026-09-23（道路に沿った参考経路）

詳細: [docs/road-routes-check.md](docs/road-routes-check.md)

### Added

- レビュー画面（`review.html`）に「道路に沿った参考経路」を追加（任意。`ENABLE_ROAD_ROUTES = True` の
  ときだけ。既定は `False` で、従来どおりの処理時間・成果物）。OpenStreetMap の徒歩用道路データ
  （糸満市の行政区域と周囲約2km）を OSMnx で1回だけ取得し、要支援者と候補避難所1〜3の間の道路に沿った
  最短の経路を Colab の中で算出して、経路の座標をレビューZIPへ同梱する（外部へは要支援者の情報を送らない）
- 要支援者選択時に候補1の道路経路を初期表示する（候補1を選択状態にする）。候補1の経路を算出できない
  場合も候補2・3へ自動で切り替えず、その理由を表示する。候補表の行を選ぶと候補2・3へ切り替わる。
  道路経路は実線、直線は破線、地点と道路の間の接続部分は点線で区別し、道路上の距離と接続部分の長さを
  分けて表示する
- 地点から150m以内に道路が無い・道路がつながっていない・地点が糸満市から1kmより外の場合は
  「経路を算出できません」と理由を表示する（直線を道路経路として表示しない）
- 道路データの出典（© OpenStreetMap contributors / ODbL 1.0）を、背景地図（国土地理院）とは分けて
  画面に表示し、`assets/osm-roads-NOTICE.txt` を同梱する
- 道路経路の回帰テスト（`test/test_road_routes.py`）と、実際の道路データでの確認手順
  （`experiments/road_routes/`）を追加

### Changed

- `sheltermatch.ipynb` の「結果確認・CSV出力・レビュー用HTML出力」セルを、「結果確認・CSV出力」と
  「レビュー用HTML出力」に分け、その間に「道路に沿った参考経路の算出（任意）」セルを追加。道路データの
  取得に失敗しても結果CSVは先に出力済みで、レビュー用HTMLには作成できなかった旨を表示する
- レビューHTMLの「本人と避難所の間のハザード区域（参考）」について、道路経路を表示する場合は
  直線についての判定である旨を注記する
- `review_builder.py` の受け渡し（`build_review_package` の引数 `road_routes`）を追加し、
  `REVIEW_BUILDER_API_VERSION` を 2 に上げた
- 結果CSV（`assigned_shelters.csv`）は変更なし（変更前とバイト単位で同一であることを確認済み）

## 2026-09-24（レビュー画面の見やすさの改善）

### Changed

- 候補を選んでいるときは、地図の表示範囲を選択中の候補（本人の地点・避難所・道路に沿った参考経路・
  道路までの接続部分）に合わせるようにした。遠いほかの候補のために地図が引いてしまわない（ほかの候補の
  マーカーは地図に残す）。経路を算出できない候補では、本人の地点と避難所が入る範囲にする。道路経路を
  作成しない設定・作成できなかった場合は従来どおり候補全体が入る範囲
- 選択中の候補を、地図では大きいマーカー・黒い外枠・「候補N 避難所名」の常時表示で、候補表では
  「選択中」の表示と左端の線でも示すようにした（色だけに頼らない）
- 内部の状態名（`match_status: ok` など）を画面に出さないようにした。正常な場合は表示せず、
  座標が無い場合は「座標がありません」、座標が不正な場合は「座標を確認してください」と表示する
  （想定外の値は「位置情報の状態を確認してください」）。結果CSVの `match_status` は変更なし
- 画面下部に「候補詳細をたたむ／候補詳細を表示」ボタンを追加し、経路情報・候補表・注意書きを隠して
  地図を広く使えるようにした（初期状態は表示）。候補表をスクロールしても、ボタンと選択中の候補の
  経路情報は隠れない
- 重複していた注意書きを整理した。上部には「避難所候補は最終的な割り当てではない」と「道路に沿った参考
  経路は通行できるか・安全かを確認した避難経路ではない」（道路経路を作成した場合）をまとめ、道路経路を
  作成できなかった場合もその旨を上部に1回だけ表示する。候補表の注記は、直線距離と「本人と避難所の間の
  ハザード区域（参考）」が直線についての判定であることに絞った。座標が無い場合の「候補が無い」旨も
  1か所にまとめた
- 結果CSV・候補順位・直線距離・道路経路の計算・ハザード判定は変更なし（変更前とバイト単位で同一で
  あることを確認済み）

## 2026-09-24（候補の比較と多数件のレビューの効率化）

### Changed

- 候補表の距離欄で、道路経路がある場合は「直線」と「道路上」の距離を2段で表示し、候補1〜3を見比べられる
  ようにした（列は増やさない）。道路上の距離を算出できない候補は「算出不可」とし、直線距離で代用しない。
  1000m以上は km で表示する（表示だけで、データの値は変えない）。候補の順位は従来どおり直線距離による
  ものとし、道路上の距離では並べ替えない。道路経路を作成しない設定では直線距離だけを表示する
- 地図左上に「全候補を表示」ボタンを追加し、本人の地点と候補1〜TOP_Nの避難所がすべて入る範囲へ戻せる
  ようにした。候補の選択・道路経路の表示はそのままで、次に候補を選ぶと再びその候補へ地図を寄せる。
  座標が無い要支援者では押せない
- 要支援者一覧に、既存データの事実だけによる絞り込みを追加した（「座標を確認」「ハザード該当あり」
  「道路経路算出不可あり」）。複数選んだ場合と文字検索はAND条件。ハザード判定を実施していない場合・
  道路経路を作成していない場合は、該当する絞り込みを表示しない。絞り込みで0件になった場合は、前の
  要支援者の情報・地図表示を残さない
- 候補表の行を Tab キーで移動し、Enter・Space キーで選べるようにした（マウスのクリックと同じ処理）。
  選択中の行に `aria-selected`・`aria-current` を付け、フォーカス中の行には枠を表示する
- `review_builder.py` がレビュー画面へ渡すデータに、ハザード判定を実施したかどうか（`hazard_checked`）を追加
- 結果CSV・候補順位・直線距離・道路経路の計算・ハザード判定は変更なし（変更前とバイト単位で同一で
  あることを確認済み）
